import React from 'react';
import AgeCalculator from './AgeCalculator';

function App() {
  return (
    <div style={{ color: 'black', minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <AgeCalculator />
    </div>
  );
}

export default App;
